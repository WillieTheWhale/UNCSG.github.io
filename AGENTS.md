# Repository instructions

- Read and follow `skills/apply-duncan-executive-brand/SKILL.md` for every visual,
  copy-hierarchy, component, or interaction change.
- Preserve the UNC institutional top bar and the CloudApps/OpenShift deployment
  contract unless the task explicitly changes them.
- Keep Astro statically generated and serve `dist/` through unprivileged nginx on
  port 8080.
- Remove construction-page `noindex` metadata when the full site launches.
